package algorithm.optimizedFlow;


import org.graphstream.graph.Graph;
import org.graphstream.graph.Node;

import java.util.LinkedList;
import java.util.Queue;

public class edmondsk {
    public static int edmondskarp(Graph graph, Node source, Node target) {
        Queue<Node> queue = new LinkedList<>();
        return 0;
    }

    public static long edmondskarpRtm(Graph graph, Node source, Node target) {
        return 0;
    }

    public static long edmondskarpAcc(Graph graph, Node source, Node target) {
        return 0;
    }
}
